services:
  - type: web
    name: recipe-finder
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: python run_web.py
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0